// more practice
// getSumOfPositiveElement(Array<number>) => number
// Sum of every positive element
function getSumOfPositiveElement(numArray){
  return -1
}
// getSumOfPositiveElement([ 1, -4, 12, 0, -3, 29, -150]) => 42

// getSumOfPositiveElement(Array<number>) => {mean: number, median: number}
// calculate the median and mean
function getMedianAndMean(numArray){
  return -1
}
// getSumOfPositiveElement([12, 46, 32, 64, 8]) => { mean: 32.4, median: 32 }

// getNameInitials(text: string) => string
function getNameInitials(text){
  return ""
}
// getNameInitials('George Raymond Richard Martin') => 'GRRM'